<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yue.nexttext" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAzFw5K_Aw27kSqXv_gtGLTFA6iK9K9hQM"/>
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.MessageConfigureActivity"
            android:parentActivityName=".UI.MainActivity"
            android:label="New Message"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".UI.MessageConfirmationActivity"
            android:parentActivityName=".UI.MessageConfigureActivity"
            android:label="Trigger Picker"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>